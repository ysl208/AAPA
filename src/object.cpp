#include <filesystem>
#include "../include/Object.h"

// namespace fs = std::filesystem;

class Object{

    static Object& generateFromFile(char* path){
        /**
            Reads in csv files that contain lines of objects
            e.g. "obj1,x,y,w,h"
        **/
        // fs::path(path).extension()
    }

};